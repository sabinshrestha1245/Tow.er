import 'package:flutter/cupertino.dart';

String userPickUpMarker = "origin";

BorderRadiusGeometry radius = const BorderRadius.only(
  topLeft: Radius.circular(24.0),
  topRight: Radius.circular(24.0),
);